name: 🚀 Deploy Laravel via FTP

on:
  push:
name: 🚀 Deploy Laravel via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    name: Enviar arquivos via FTP
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🚀 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v3
        with:
          server: "ftp.nexocode.com.br"
          username: "Josevitor@starttofinish.com.br"
          password: "29_Zezao_DevFull"
          server-dir: /home1/usuario/public_html/starttofinish.com.br
          exclude: |
            **/.git*
            **/.env
            **/node_modules/**
            **/vendor/**
            **/storage/**
            README.md
            .editorconfig
            .styleci.yml
            .github/
            tests/
